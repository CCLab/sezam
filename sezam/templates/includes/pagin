    <div id="authority_pagination" class="pagination pagination-mini">
      {% if xpaginate.total_pages > 1 %}
      <ul>
        {% if xpaginate.has_previous %}
        <li><a href="{{ xpaginate.uri }}{{ xpaginate.previous }}">«</a></li>
          {% if xpaginate.has_jump_previous %}
            {% for a in xpaginate.previous_page_jump %}
        <li><a href="{{ xpaginate.uri }}{{ a }}">{{ a }}</a></li>
        <li class="disabled"><a>...</a></li>
            {% endfor %}
          {% endif %}
          {% for a in xpaginate.previous_pages %}
        <li><a href="{{ xpaginate.uri }}{{ a }}">{{ a }}</a></li>
          {% endfor %}
        {% endif %}
          
        <li class="active"><a href="#">{{ xpaginate.current }}</a></li>
          
        {% if xpaginate.has_next %}
          {% for a in xpaginate.foward_pages %}
        <li><a href="{{ xpaginate.uri }}{{ a }}">{{ a }}</a></li>
          {% endfor %}
          {% if xpaginate.show_last %}
        <li class="disabled"><a>...</a></li>
        <li><a href="{{ xpaginate.uri }}{{ xpaginate.total_pages }}">{{ xpaginate.total_pages }}</a></li>
          {% endif %}
        <li><a href="{{ xpaginate.uri }}{{ xpaginate.foward }}">»</a></li>
        {% endif %}
      </ul>
      {% endif %}
    </div>
