    <div class="row">
      <div class="span8">
        <h2>{{ authority.name }}</h2>
      </div>
    </div>

    <div class="row">
      <div class="span8">
        <p>{% if authority.web_site %} Official web-site: {{ authority.web_site|urlize }}{% endif %}</p>
        <p>{% if authority.web_site1 %} Buletin of public information: {{ authority.web_site1|urlize }}{% endif %}</p>
      </div>
    </div>

    <div class="row">
      <div class="span3">
        <p><strong>Address:</strong>
          <address>
          {{ authority.address_street }} {{ authority.address_num }}
          <br>{{ authority.address_postalcode|slice:":2" }}-{{ authority.address_postalcode|slice:"2:5" }} {{ authority.address_city }}</br>
          </address>
        </p>
      </div>
      <div class="span2">
        <p>
          <strong>Telephones:</strong>
          <address> ({{ authority.tel_code }}) {{ authority.tel_number|slice:":3" }} {{ authority.tel_number|slice:"3:5" }} {{ authority.tel_number|slice:"5:" }} {{ authority.tel_internal|default:'' }}
          {% if authority.tel1_number %}
          <br>({{ authority.tel1_code }}) {{ authority.tel1_number|slice:":3" }} {{ authority.tel1_number|slice:"3:5" }} {{ authority.tel1_number|slice:"5:" }} {{ authority.tel1_internal|default:'' }}
          {% endif %}
          {% if authority.tel2_number %}
          <br>({{ authority.tel2_code }}) {{ authority.tel2_number|slice:":3" }} {{ authority.tel2_number|slice:"3:5" }} {{ authority.tel2_number|slice:"5:" }} {{ authority.tel2_internal|default:'' }}
          {% endif %}
          </address>
        </p>
      </div>
      {% if authority.fax_number %}
      <div class="span2">
        <p><strong>Fax:</strong>
          <address> ({{ authority.fax_code }}) {{ authority.fax_number|slice:":3" }} {{ authority.fax_number|slice:"3:5" }} {{ authority.fax_number|slice:"5:" }}</address>
        </p>
      </div>
      {% endif %}
    </div>

    {% if authority.official_lastname %}
    <div class="row">
      <div class="span8">
        <p><strong>Official:</strong>
          {{ authority.official|default:'' }}  <em>{{ authority.official_name|default:'' }} {{ authority.official_lastname|default:'' }}</em></p>
      </div>
    </div>
    {% endif %}

    {% if authority.description %}
    <div class="row">
      <p><strong>Description</strong></p>
      <p class="small">{{ authority.description|default:''|urlize }}</p>
    </div>
    {% endif %}

    {% if authority.notes %}
    <div class="row">
      <p><strong>Notes</strong></p>
      <p class="small">{{ authority.notes|default:'' }}</p>
    </div>
    {% endif %}

    <div class="row span8">
      <div class="btn-group">
        <a class="btn btn-small btn-info" href="#"> Follow this authority</a>
        <a class="btn btn-small btn-primary" href="/request/{{ authority.slug }}/"><i class="icon-envelope icon-white"></i> Make request</a>
      </div>
    </div>
