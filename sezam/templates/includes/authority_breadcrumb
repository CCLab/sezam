      <ul class="breadcrumb">
        {% for category in categories %}
        <li><a href="/authority/list/{{ category.id }}">{{ category.name|title }}</a> <span class="divider">/</span></li>
        {% endfor %}
        <li class="active">{{ authority.name|slice:":20" }}{% if authority.name|length > 20 %}...{% endif %}</li>
      </ul>
