      <ul class="breadcrumb">
        {% for category in categories %}
        <li><a href="/authority/list/{{ category.id }}">{{ category.name|title }}</a> <span class="divider">/</span></li>
        {% endfor %}
        <li class="active">{{ result.name|slice:":20" }}{% if result.name|length > 20 %}...{% endif %}</li>
      </ul>
